@model RHPsicotest.WebSite.Models.Permission

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="row d-flex justify-content-center mt-5">
    <div class="col-12 col-sm-10 col-md-8 col-xl-5">
        <h1 class="text-center fw-bold">Editar Permiso</h1>

        <form asp-action="Edit" class="p-3">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdPermission" />
@*            <div class="form-group my-2">
                <label asp-for="IdModule" class="control-label fw-bold fs-5">Seleccionar Módulo</label>
                <select asp-for="IdModule" class="form-control" asp-items="@(new SelectList(ViewBag.Modules, "IdModule", "ModuleName"))">
                    <option>Seleccione</option>
                </select>
                <span asp-validation-for="IdModule" class="text-danger"></span>
            </div>*@
            <div class="form-group my-3">
                <label asp-for="PermissionName" class="control-label fw-bold fs-5"></label>
                <input asp-for="PermissionName" class="form-control" />
                <span asp-validation-for="PermissionName" class="text-danger"></span>
            </div>
            <div class="form-group text-center mt-3">
                <input type="submit" value="Aceptar" class="btn btn-primary col-5" />
            </div>
        </form>
    </div>
</div>

<div class="text-end">
    <a asp-action="Index" class="btn btn-dark me-5">Volver a la Lista</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
